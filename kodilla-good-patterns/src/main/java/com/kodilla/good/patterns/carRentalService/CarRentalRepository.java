package com.kodilla.good.patterns.carRentalService;

import java.time.LocalDateTime;

public class CarRentalRepository implements RentalRepository {
    public void createCarRental(User user, LocalDateTime carRentFrom, LocalDateTime carRentTo) {
    }

    @Override
    public void createRental(User user, LocalDateTime from, LocalDateTime to) {

    }
}
